# benben 服务器环境变量模板
# 用法：复制到 /etc/benben/env，填入真实值，chmod 600
# 切勿将此文件（含真实密钥）提交进 git
#
# settings.py 由环境变量驱动，以下为迁移必需项（缺失将导致 Django 无法启动）
ALPHAVANTAGE_API_KEY=your_alpha_vantage_key_here
SECRET_KEY=your_django_secret_key_here
# 可选（如现服务器 env 中还有其它键，按需追加）：
# DEBUG=False
# ALLOWED_HOSTS=127.0.0.1,你的域名
