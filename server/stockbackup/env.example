# 独立备份接收端环境变量模板
# 用法：复制到 /etc/stockbackup.env，填入真实 token，chmod 600
# 切勿将此文件（含真实 token）提交进 git；真实 token 只存在于服务器 /etc/stockbackup.env
#
# token 必须与 CloudBase dbBackup 云函数发送 X-Backup-Token 的值完全一致。
# 历史取值可在服务器原 benben/views.py 的 BACKUP_WEBHOOK_TOKEN 兜底字面量处查到。
BACKUP_WEBHOOK_TOKEN=__SET_ME__
